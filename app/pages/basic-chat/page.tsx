'use client';

import React from 'react';
import { Chat } from '../../components/chat/chat';

const Home = () => {
    return (
        <main className='container'>
            <Chat />
        </main>
    );
};

export default Home;
