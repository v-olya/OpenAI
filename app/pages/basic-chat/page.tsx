import { Chat } from '../../components/chat/chat';

const BasicChat = () => {
    return (
        <main className='container'>
            <Chat chatType='basic' />
        </main>
    );
};

export default BasicChat;
